# Migration `20210128172132-adds-comment-model`

This migration has been generated by Nirmalya Ghosh at 1/28/2021, 10:51:32 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "Comment" (
"id" SERIAL,
    "body" TEXT NOT NULL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "fileId" INTEGER NOT NULL,
    "userId" INTEGER NOT NULL,

    PRIMARY KEY ("id")
)

ALTER TABLE "Comment" ADD FOREIGN KEY("fileId")REFERENCES "File"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Comment" ADD FOREIGN KEY("userId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20210128042039-adds-url-to-file-model..20210128172132-adds-comment-model
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgres"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -20,8 +20,9 @@
   email          String    @unique
   hashedPassword String?
   role           String    @default("user")
   sessions       Session[]
+  comments       Comment[]
 }
 model Session {
   id                 Int       @id @default(autoincrement())
@@ -45,12 +46,24 @@
   files     File[]
 }
 model File {
+  id        Int       @id @default(autoincrement())
+  name      String
+  url       String
+  createdAt DateTime  @default(now())
+  updatedAt DateTime  @updatedAt
+  project   Project   @relation(fields: [projectId], references: [id])
+  projectId Int
+  comments  Comment[]
+}
+
+model Comment {
   id        Int      @id @default(autoincrement())
+  body      String
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
-  project   Project  @relation(fields: [projectId], references: [id])
-  projectId Int
-  name      String
-  url       String
+  file      File     @relation(fields: [fileId], references: [id])
+  fileId    Int
+  author    User     @relation(fields: [userId], references: [id])
+  userId    Int
 }
```


